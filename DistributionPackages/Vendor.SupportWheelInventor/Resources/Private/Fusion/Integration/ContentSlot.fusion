prototype(Vendor.SupportWheelInventor:ContentSlot) < prototype(Neos.Neos:ContentComponent) {
    renderer = PackageFactory.AtomicFusion.PresentationObjects:Slot {
        presentationObject = ${SupportWheelInventor.ContentSlot.forContentNode(node, documentNode, site, Neos.Node.subgraphForNode(node), renderingMode.isEdit)}
    }
}
